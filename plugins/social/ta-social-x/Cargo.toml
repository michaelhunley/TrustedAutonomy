[workspace]

[package]
name = "ta-social-x"
version = "0.1.0"
edition = "2021"
description = "X (Twitter) social media adapter plugin for Trusted Autonomy — create drafts and schedule posts via X API v2 (OAuth2 PKCE)"
license = "Apache-2.0"

[[bin]]
name = "ta-social-x"
path = "src/main.rs"

[dependencies]
serde = { version = "1", features = ["derive"] }
serde_json = "1"
reqwest = { version = "0.12", default-features = false, features = ["json", "rustls-tls", "blocking"] }
