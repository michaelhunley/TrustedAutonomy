[package]
name = "ta-daemon"
version = "0.1.0"
edition = "2021"
description = "Long-running local service for Trusted Autonomy"
license = "Apache-2.0"

[dependencies]
clap = { workspace = true }
tracing = { workspace = true }
tracing-subscriber = { workspace = true }
